<div class="container">
  <div class="row">
    <div class="col-md-2">
      <h2>Welcome to EmberNote</h2>
    </div>
    <div class="col-md-10">
      {{#if user}}
        Hello, {{user.name}}
      {{/if}}
      {{#unless user}}
        {{#link-to 'register'}}Register{{/link-to}}&nbsp;
        {{#link-to 'login'}}Login{{/link-to}}
        <p/>
      {{/unless}}
    </div>
  </div>
  <div class="row">
    <div>
      {{outlet}}
    </div>
  </div>
</div>
